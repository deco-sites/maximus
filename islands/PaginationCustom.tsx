import Component from "$store/components/search/PaginationCustom.tsx";
import type { Props } from "$store/components/search/PaginationCustom.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
