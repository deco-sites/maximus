import Component from "$store/components/product/Price.tsx";
import type { Props } from "$store/components/product/Price.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
