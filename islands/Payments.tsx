import Component from "$store/components/product/Payments.tsx";
import type { Props } from "$store/components/product/Payments.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
