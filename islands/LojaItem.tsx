import Component from "$store/sections/Content/LojasItem.tsx";
import type { Props } from "$store/sections/Content/LojasItem.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
